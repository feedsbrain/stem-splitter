# torch, torchaudio, torchvision, and rocm are NOT installed from here.
# They're ROCm nightly builds and are installed by "run.bat --setup" via:
#   pip install --pre torch torchaudio torchvision rocm --index-url <url>
# <url> comes from stem-splitter.ini's [rocm] index_url (default:
# gfx103X-dgpu) - edit that to target a different AMD GPU architecture.

yt-dlp==2026.7.4
demucs==4.0.1
soundfile==0.14.0
